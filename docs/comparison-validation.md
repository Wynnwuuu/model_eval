# 比较页验证记录

验证日期：2026-09-14。

## 数据与构建

- `npm test` 通过：原分析展示、独立盲评逻辑、新增比较数据验证。
- `npm run build` 通过 TypeScript 与 Vite 构建。完整音频分析随产物打包；Vite 对较大的文本数据包给出体积提示，无构建错误。
- 导入脚本 `--check` 通过：当前快照重新对照原始前半 CSV 和后半 ZIP，30 题、90 份输出与生成数据一致；旧后半 CSV 的 42 条已评名次与 ZIP 一致。
- 独立验证覆盖 90 份输出 SHA-256、30 份 WAV SHA-256、90 条真实模型与 Prompt 的名次关联、数组顺序逆转时仍保持正确映射、17 类非法数据拒绝。
- 原 `src/data/evaluation.json` 保持字节一致，原工作目录仍在原分支且没有改动。

## 实际页面

通过浏览器逐题选择全部 30 题，在“完整原文”分区检查全部 90 张卡片：

1. DOM 中的第 1、2、3 名与源数据名次一致。
2. 卡片真实模型标题与 VariantID 同时匹配该名次。
3. 每份实际渲染原文的长度与 FNV-1a 摘要匹配对应源文本；数据文件另以 SHA-256 校验。
4. 当前题目标题和选择控件匹配 CaseID。

另验证：

- 第 30 题链接直达与刷新恢复正确，排名为 Plus、Pro、Flash。
- 最后一题音频点击后进入播放状态，并可暂停。
- 音乐类别筛选只展示对应 5 题，切回全部类别可恢复 30 题。
- 实测 CSS 内容宽度约 404 px 时卡片纵向排列，无横向溢出；约 1487 px 时卡片为三列，无横向溢出。
- 页面使用固定快照，不读取或更新个人评审存储；原盲评入口为 `?view=blind`。

本次未重复运行完整旧版 Playwright E2E 套件；旧盲评测试的入口已相应改为 `?view=blind`。新功能的数据验证与上述浏览器核对已完成。
