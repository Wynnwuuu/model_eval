apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

patches:
  - path: patch-labels.yml
  - path: patch-config.yml

labels:
  - includeSelectors: false
    pairs:
      environment: dev
